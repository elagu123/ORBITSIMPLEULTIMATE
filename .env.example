# =============================================================================
# ENVIRONMENT VARIABLES CONFIGURATION
# =============================================================================

# AI Services
GEMINI_API_KEY=your_google_gemini_api_key_here

# Backend Services
VITE_BACKEND_URL=http://localhost:3001

# Error Tracking (Sentry)
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Application Metadata
VITE_APP_VERSION=1.0.0
VITE_APP_NAME=Orbit Marketing Platform

# Analytics (Coming Soon)
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
# VITE_MIXPANEL_TOKEN=your_mixpanel_token

# Development Settings
NODE_ENV=development

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env.local:
#    cp .env.example .env.local

# 2. Fill in your actual API keys and configuration values

# 3. Never commit .env.local to version control - it's already in .gitignore

# 4. For production deployment, set these environment variables in your hosting platform:
#    - Vercel: Project Settings > Environment Variables
#    - Netlify: Site Settings > Environment Variables  
#    - Docker: Use --env-file or docker-compose environment section

# =============================================================================
# SENTRY SETUP GUIDE
# =============================================================================

# 1. Create a Sentry account at https://sentry.io
# 2. Create a new project for React
# 3. Copy the DSN from the project settings
# 4. Paste it as VITE_SENTRY_DSN value above
# 5. The DSN format looks like: https://abc123@o123456.ingest.sentry.io/123456

# =============================================================================
# SECURITY NOTES
# =============================================================================

# - API keys in VITE_ variables are exposed to the client
# - Use server-side proxy for sensitive API calls
# - GEMINI_API_KEY should be moved to backend in production
# - Sentry DSN is safe to expose (it's public for error collection)